# 第一阶段开发

## 目标

1.设计好项目整体的文件夹结构.这个项目以后的部署方式是git clone 到别的项目里,所以要把文件夹结构设计好,方便以后维护和使用.
2.设计好项目的内部测试项目,一个fastapi的python项目,附带pytest的自动化测试代码,这个项目的目的是为了测试这个项目的功能,同时也可以作为这个项目的demo.
3.实现PCL(program check list)的生成功能,这个功能是读取现在存在的pytest自动化测试代码,分析代码的结构和内容,然后生成一个PCL.这个PCL是HTML模版的格式,API会依据模版生成带数据的HTML文件,并支持以PDF的格式输出.

## 细节计划

### 1.设计好项目整体的文件夹结构.
这个我允许你设计,我的需求是结构要清晰,代码,demo,skill文件要分开,同时要有一个README.md文件来说明这个项目的功能和使用方法.

### 2.设计好项目的内部测试项目

简单的CRUD的fastapi项目,附带pytest的自动化测试代码.附带一个sqlite数据库


### 3.实现PCL(program check list)的生成功能

#### 日本的PCL是这样的结构

title: Program Check List
file: 测试对象的文件名
method: 测试对象的函数/方法名

testcase: 测试用例的名字,简短的描述这个测试的目的
testname:自动化测试里对应的测试名
testcasecode:这个测试用例的编号

input: 输入的参数和数据
output: 预期的输出结果

type: 正常值(N),边界值(I),异常值(E)

以下是不需要填入内容,由人填写的空间
date: 测试完成的日期


#### 日本的PCL需要是这样的外观

PCL是单元测试的说明文档,后端开发时日本的单元测试会对每一个输入值(参数)进行测试,会对正常值,边界值,异常值进行测试.
但是每个参数都是正常值的测试只需要一次,边界值和异常值测试时,只需改变一个参数,其余参数维持正常值

这样PCL的结构就是一个表格,输入什么时行,测试用例是列,对应的测试用例用了这个输入值,就会在这个单元格画圆圈.
输出的结构也是一样的.

#### UI设计

类似表格,title区可以厚一些,使用蓝色背景.表格区使用白色背景(你要注意PCL本来是excel文件来的,不同公司的内部文件有不同的风格)
网页上可以用不同的颜色深浅来区分格子,但是要考虑到这个导出PDF很可能是黑白印刷,要避免打印出黑色背景搭配黑色字.也就是背景都是浅色.(表格区)

#### 数据结构设计

测试用例用json来保存,生成html时填入模版内.同时存在一个cli读取json来查询这个测试用例是什么.

#### API设计
调用表格生成是CLI
导出为PDF也是CLI
CLI需要help之类的设计
这个功能的输入风格是 teaforge pcl generate --path xxx.py --output xxx.html
或者 teaforge export --path xxx.html --output xxx.pdf
或者 teaforge get --path xxx.html --testcase xxx 这样会返回自然语言描述的测试用例内容,以便于AI能根据这个内容来生成自动化测试代码.

generate是生成html文件的命令,export是把html文件转换成pdf文件的命令.